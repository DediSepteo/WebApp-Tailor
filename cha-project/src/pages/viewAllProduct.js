import ViewAll from "../components/viewAll";

const ViewAllProduct = ({ type }) => {
    return (
        <ViewAll category="Product" type={type}></ViewAll>
    )
}

export default ViewAllProduct;